# Mission Repair Gate

## Purpose
Decide whether work can finalize or must loop back.

## Rules
- Use `APPROVED_TO_W4` only when acceptance evidence is complete.
- Use `LOOP_BACK_TO_W1` when repair tasks can be generated.
- Use `NEEDS_HUMAN_CONFIRMATION` when requirements conflict or evidence cannot resolve the risk.
