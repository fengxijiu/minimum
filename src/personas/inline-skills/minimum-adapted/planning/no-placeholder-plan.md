# No Placeholder Plan

## Purpose
Reject plans that are not executable by a worker.

## Rules
- Do not emit `TBD`, `TODO`, `handle edge cases`, or "similar to previous task".
- Use concrete files, commands, acceptance checks, and blocked conditions.
- If a detail is unknown, create a perception or context-building task instead of guessing.
