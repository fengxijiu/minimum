# Green Verification

## Purpose
Ensure completion claims are backed by test output.

## Rules
- Test runner reports must include command, exit code, passed count, failed count, and failure summary.
- If no test is run, report an explicit waiver reason.
